# Production triage ledger

Persistent fingerprints found by the daily check-in. Add a row only after evidence appears in an archived report.

This file is committed to a public repository. Never paste user feedback text here; reference the check-in date instead.

| Fingerprint | Surface | Verdict | First seen | Last count | Evidence and next condition |
| --- | --- | --- | --- | ---: | --- |
| `scriptThrewException` with no matching [Sentry](https://sentry.io) issue | Worker `unlighthouse-dev` | resolved | 2026-08-13 | 14 requests / 24h | [Cloudflare](https://cloudflare.com) invocation analytics recorded 14 thrown exceptions against 3,063 successes in the 24h to 2026-09-11T22:00Z, while the Sentry query for unresolved issues seen in the same window returned none. The attribution fixes shipped 2026-09-10 (#76, #81, #90) have not produced a single matching issue. Exit condition met: three consecutive clean windows ending 2026-09-14T22:06Z showed the count at zero with no matching Sentry issue. |
| `inp` 502 on one target URL | Worker `unlighthouse-dev` `inp` tool | watch | 2026-09-05 | 2 of 5 runs / 24h | The 2026-09-10 check-in crossed the quarter-broken threshold: 2 of 5 statused `inp` runs (40%) failed 502 on one distinct target, fingerprint count 2 → 4, latest hit 2026-09-10T14:27Z after the deploy. Hypothesis: PSI deterministically fails to measure that one URL (heavy page or bot blocking); one repeated target is a UX finding, not an outage. Both hits stayed out of Sentry, so the expected-upstream-failure tagging from #80 held. Exit condition: one check-in where the target succeeds, the errors spread across distinct targets, or a named Sentry issue attributes them. |
| `cwv-check` 502 on one target URL | Worker `unlighthouse-dev` `cwv-check` tool | watch | 2026-09-15 | 1 hit / 24h | First `cwv-check` error row in any archive: one hit at 2026-09-15T07:01Z against one distinct target in the 24h to the 2026-09-15T07:02Z check-in. Hypothesis: upstream [PageSpeed Insights](https://developers.google.com/speed/docs/insights/v5/about) fails to measure that one URL (heavy page or bot blocking); one hit on one target is a UX finding, not an outage. The hit stayed out of [Sentry](https://sentry.io). Exit condition: three consecutive clean windows, the errors spreading across distinct targets, or a named Sentry issue attributes them. |
| `internalError` with no matching [Sentry](https://sentry.io) issue | Worker `unlighthouse-dev` | resolved | 2026-09-11 | 66 requests / 24h | First appearance in any archived day: [Cloudflare](https://cloudflare.com) invocation analytics recorded 66 `internalError` outcomes against 3,063 successes in the 24h to 2026-09-11T22:00Z. Sentry returned no new or recurring issues for the window, so the outcome is Cloudflare-side and cannot be attributed from app code. Exit condition met: three consecutive clean windows ending 2026-09-14T22:06Z showed the count at zero. |
| `page-size` 502 recurring after the 2026-09-10 fix deploy | Tool `page-size` | watch | 2026-09-07 | 6 hits on 1 distinct target / 24h | Reopened by the 2026-09-23 check-in: 6 hits against one new target (`www.accu-chek.com`) between 2026-09-23T09:40:13Z and 11:31:15Z, after the row resolved 2026-09-15 on three clean windows ending 2026-09-14T22:06Z. Hypothesis: upstream [PageSpeed Insights](https://developers.google.com/speed/docs/insights/v5/about) fails to measure that one URL (heavy page or bot blocking); one repeated target is a UX finding, not an outage. Exit condition: three consecutive clean windows or a named [Sentry](https://sentry.io) issue attributes them. |
| `lcp` 422 from upstream [PageSpeed Insights](https://developers.google.com/speed/docs/insights/v5/about) | Tool `lcp` | resolved | 2026-09-11 | 1 hit | First `lcp` error row in any archive: one hit at 2026-09-11T10:33Z against one distinct target. Hypothesis: upstream PSI rejects that URL as unprocessable (422), a UX finding rather than an incident. Exit condition met: three consecutive clean windows ending 2026-09-14T22:06Z showed zero new hits and no second target. |
| `pagespeed-insights` 502 on one target URL | Worker `unlighthouse-dev` `pagespeed-insights` tool | watch | 2026-09-15 | 2 hits on 1 distinct target | First appearance in any archive: the 2026-09-15 check-in recorded 2 hits in `d1.toolErrorFingerprints` at 2026-09-15T07:07:18Z and 07:10:11Z against one distinct target. Hypothesis: upstream [PageSpeed Insights](https://developers.google.com/speed/docs/insights/v5/about) deterministically fails that one URL (heavy page or bot blocking); one repeated target is a UX finding, not an outage. Exit condition: one clean check-in or a named Sentry issue. |
